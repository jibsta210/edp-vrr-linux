# Add the EDID firmware to initramfs so it's available at early boot.
# Edit /etc/mkinitcpio.conf and add to the FILES array:
#
#   FILES=(/lib/firmware/edid/vrr.bin)
#
# Then rebuild: sudo mkinitcpio -P
