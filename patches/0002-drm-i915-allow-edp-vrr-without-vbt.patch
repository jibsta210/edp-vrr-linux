--- drivers/gpu/drm/i915/display/a/drivers/gpu/drm/i915/display/intel_vrr.c	2026-04-14 23:40:12.801591938 -0400
+++ drivers/gpu/drm/i915/display/intel_vrr.c	2026-04-14 23:40:12.803298761 -0400
@@ -47,7 +47,12 @@
 	 */
 	switch (connector->base.connector_type) {
 	case DRM_MODE_CONNECTOR_eDP:
-		if (!connector->panel.vbt.vrr)
+		/*
+		 * Prefer VBT flag, but fall back to EDID monitor range
+		 * for panels where OEM firmware omits the VBT VRR flag.
+		 */
+		if (!connector->panel.vbt.vrr &&
+		    !(info->monitor_range.max_vfreq - info->monitor_range.min_vfreq > 10))
 			return false;
 		fallthrough;
 	case DRM_MODE_CONNECTOR_DisplayPort:
